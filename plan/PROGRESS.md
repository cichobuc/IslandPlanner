# Denník postupu

## 2026-09-20 · Návrh
- Zadanie od používateľa + 12 upresňujúcich otázok (rok flexibilný, Bratislava, 3–4 dospelí, bezplatné API, self-transfer, porovnanie scenárov, Vercel+Supabase zadarmo, iPad, okruh podľa ceny, všetky záujmy, úsporná strava, tmavý aurora dizajn, magic link + zdieľanie, denný cron + e-mail, SK+CS).
- Doplnené požiadavky počas práce: šablóny trás/konfigurácií pre ostatných používateľov + editovateľnosť všetkého + história; popis/vstupné/trvanie pri každej atrakcii; dron (zóny, pravidlá, checklist); menej známe miesta; sezónnosť.
- Napísané: README, docs 01–07, stránky 00–12, plán, backlog, ADR.
- Dizajn: Claude Design canvas https://claude.ai/artifact/HYKyfB8V1VcKnJiuc9iNx7 (artboardy tablet + mobil).

- Prerobené na jednotný zoznamový vzor (3 obrazovky + 8 krokov + sheety) podľa spätnej väzby; staré `docs/stranky/` nahradené `docs/obrazovky/`; dizajn nakreslený nanovo.

- Vetvenie v kroku 3 (Auto / Karavan / Bez auta → 04 Ubytovanie/Kempy/Základňa, 05 Itinerár/Výlety), reťazenie krokov (vstupy, „Pokračovať → 0N").
- Prechod krok po kroku: zlepšenia a checklisty v každom `krok-*.md`, súhrn v `docs/08-prechod-a-checklisty.md`, doplnený dátový model a engine (reálne časy jazdy, noc 1/posledná, timeline platieb, balenie).
- Dizajn v3: svetlý editorial (používateľ nechce tmavý ani „AI glass").

- Konektivita overená od nuly (web, 20. 9. 2026): docs/04 prepísané ako jednotný katalóg konektorov; odstránené Amadeus/Hotellook/Kiwi; pridané gflights (overenie pre 4 os.), viator (túry), gasvaktin (palivo), oficiálna dron mapa; Camping Card do 15. 9.; jednotná terminológia v celej dokumentácii (ADR-011).

- Doplnené: kontá s dočasným heslom + profil preferencií (00a), návrh ideálneho mesiaca, režim Ja/Skupina s kompromismi, delenie skupinových nákladov (1 auto z BA), modul „Vziať z Bratislavy“ so seed cenami a colnými limitmi (ADR-012).

- Dizajn v4 „Liquid Glass × Island" (overené UI trendy 2026) → v5 „Cesta ako trasa": spine so stanicami, jeden otvorený krok s časťami Vstupy → Voľba → Vyplýva, hero „Odhad celej cesty", obrazovka „Ako to funguje". 9 artboardov.

- Dizajn v6: po vykreslení v Playwrighte (v4/v5 vyzerali ako generický SaaS so sklom) prerobené na čisté produktové UI – Postup vľavo, obsah kroku vpravo; každý artboard skontrolovaný ako PNG (`scratchpad/pw/shots`).

- Audit celého návrhu (docs/09): 6 kritických opráv (route_matrix, pokrytie leteniek spike, gflights fallback, itinerár per vetva, concurrency, is_manual), 12 dôležitých, MVP do 02/2027 (~50 večerov), plný rozsah ~110.

- Repo https://github.com/cichobuc/IslandPlanner založené, prvý commit = celý návrh (32 súborov).

## Ďalší krok
- Účty: Supabase, Vercel, Travelpayouts, ORS, Resend → blok 1.1 šprintu.
- Prejsť dokumentáciu, opraviť/potvrdiť seed ceny, začať Fázu 0.

## 2026-09-20 · Šprint deň 1
- Rozhodnutia pred štartom: MVP **bez** vetvy „Bez auta" (stub karta), správca inštancie = l.pjecha@gmail.com, pnpm + Vercel CLI. Kľúče (Supabase, Travelpayouts, ORS, Resend) zatiaľ nie sú – blok 1.1 ide bez nich, doplnia sa pred 1.2.
- **Blok 1.1 hotový (lokálne):** Next.js 15.5 + React 19 + TS strict + Tailwind 4 (tokeny v6 v `globals.css` cez `@theme`), next-intl 4 (`/sk`, `/cs` s fallbackom na sk), Drizzle + drizzle-kit config, Supabase SDK, Zod, Vitest, Prettier, ESLint, Playwright (Chromium), `.env.example`, fonty Sora + Instrument Sans (next/font). Layout `src/app/[locale]` (nie root `app/` – štandard Next so `src/`). `pnpm build` prešiel, úvodná stránka vykreslená Playwrightom (820×1180) a skontrolovaná. **Nasadené: https://islandplanner.vercel.app** (import z GitHubu, `/` → `/sk`, 200). Blok 1.1 = hotové.
- **Blok 1.2 hotový:** Drizzle schéma podľa docs/03 – 44 tabuľok, 30 enumov, v `src/db/schema/{_shared,users,reference,trips,flights,itinerary,templates}.ts`; `Money` ako jsonb `{amount,currency,source,fetchedAt,provider}` (+ `numeric` stĺpec tam, kde SQL radí/agreguje: `flight_options.total_group_amount`, `price_snapshots.total_amount`); `is_manual` (K6), `scenario_key` (K4), `updated_at` + trigger (K5), `route_matrix` (K1), `profiles.must_change_password` + `is_admin`. RLS: 98 politík definovaných v TS (`pgPolicy`), pomocné funkcie `is_trip_member/editor/owner`, `is_admin`, `shares_trip_with` (security definer) + trigger `trips_add_owner_member` v `src/db/sql/functions.sql`. **Zistenie:** `drizzle-kit push` vytvára politiky bez USING/WITH CHECK → `src/db/apply-sql.ts` ich po pushi pregeneruje zo schémy a skontroluje (`pnpm db:push` = push + apply). Overené smoke testom v transakcii (owner trigger, nečlen 0 riadkov, viewer číta/neupraví, anon nič). Supabase projekt `ldqkygspqlfoppqioqyt` (eu-west-2), DB pripojenie cez pooler.
- **Blok 1.3 hotový:** Auth cez Supabase (e-mail + heslo, `@supabase/ssr`): `src/lib/supabase/{server,client,admin,middleware}.ts`, `src/lib/auth/{session,password,create-user}.ts`. Middleware = next-intl + obnova session + presmerovania (neprihlásený → `/prihlasenie?next=`, `must_change_password` → `/zmena-hesla`). Príznak je v `profiles.must_change_password` **aj** v `auth.app_metadata` (middleware bez DB dotazu). Obrazovky `/prihlasenie`, `/zmena-hesla` (min. 10 znakov), `/sprava/pouzivatelia` (len správca: založenie konta → dočasné heslo zobrazené raz; existujúci e-mail = reset), `POST /api/admin/users` (403 pre nesprávcu), `POST /api/auth/signout`, stub `/profil`. `pnpm admin:bootstrap "Meno"` založí/resetne správcu z `ADMIN_EMAIL`. E-mail cez Resend pripravený v `src/lib/email.ts` (bez kľúča sa nepošle, heslo sa zobrazí). Overené e2e skriptom `tests/e2e/auth-flow.mjs` (iPad + iPhone viewport, PNG skontrolované): zlé heslo, vynútená zmena, krátke heslo, založenie Petra, Peter 404/403 na správu, opätovné prihlásenie novým heslom. Poučenia: React 19 resetuje formulár po akcii (e-mail treba vrátiť v stave); presmerovanie po prihlásení rieši action, nie middleware (RSC navigácia neaktualizuje URL); stránky so session bez `generateStaticParams` (inak SSG).
  Vercel: projekt prepojený cez CLI (`vercel link`), 6 env premenných nahraných skriptom z `.env` (anon key ako `--type config`, ostatné secret). Produkcia https://islandplanner.vercel.app – prihlásenie s dočasným heslom overené smoke testom (presmerovanie na /zmena-hesla), testovací účet zmazaný.
- **Blok 1.4 hotový:** `/profil` – formulár podľa 00a (O mne, Vodič, Dron, Čo chcem vidieť 10×0–3, Ako cestujem, Letiská, Doklady; „Kedy môžem" = v1.1). Server action so Zod schémou (`src/features/profile/schema.ts`, FormData → jsonb polia `driver_licence`, `drone`, `interests`, `docs`), „Uložiť" priebežne, „Hotovo" nastaví `completed_at` + `app_metadata.profile_completed`; middleware vedie nevyplnený profil na `/profil`. UI primitívy `Segmented`, `ChipGroup`, `FieldRow`, `Section`, `Select` (čisté CSS, bez JS). `travelerFromProfile()` pripravený na prenos do kroku 01 (blok 2.3). Overené e2e: nevyplnený → presmerovanie, uloženie skontrolované v DB (jsonb polia), Hotovo → domov, reload drží hodnoty; PNG iPad + iPhone skontrolované. **Pozn.:** existujúce kontá (l.pjecha) budú po prihlásení presmerované na profil, kým nedajú „Hotovo".
- **Blok 1.5 hotový:** Engine `src/engine` (čisté TS, bez IO): `types` (TripSnapshot/BudgetResult), `money` (Money jsonb, ISK→EUR, propagácia pôvodu, konfidencia, rozsah ±15 %), `time` (tz KEF/BA cez Intl, sezóny), `ageRules` (vek v deň návštevy, pravidlá per person/vehicle/group, varianty), `split` (group/person/vehicle/custom, cesta na letisko auto vs. bus), `presets` (regióny, 5 presetov s km, rozdelenie nocí, seed tabuľky izieb/kempov/vozidiel z docs/07), `food` (kuchynka ráno/večer, úrovne, prvý/posledný deň podľa letu), `lodging` (izba/Airbnb/kemp, Camping Card rozhodnutie, varovania), `transport` (prenájom, poistenia, palivo ×1,05, tunel, kontroly vozidla), `transportMode` (odhad vetiev Auto/Karavan/Bez auta pre krok 03), `cascade` (odvodené hodnoty z letu vrátane parkovania/prenájmu, kostra dní z presetu, mení len `is_manual=false`, návrhy pre ručné, diff), `budget` (7 kategórií, scenáre car/camper, perTraveler, rezerva, odporúčanie), `timeline` (stub Kedy platiť). **46 testov Vitest zelených** (vrátane prípadov z docs/05: senior 67/68, kaskáda 9→11 dní so zamknutým dňom, kemp vs. hotel strava, Σ kategórií = skupina). Test odhalil chybu: ručné položky `person` sú na osobu → × pax. Itinerár z POI (`itineraryGen`) = blok 2.7, `flightCombos` = blok 1.9 (kaskáda má generátor ako injektovanú funkciu).
- **Blok 1.6 hotový:** `seed/*.json` (čitateľné, commitované) + `src/db/seed/index.ts` (idempotentné upserty, `pnpm seed` ~ 15 s): 15 letísk (5 origin + KEF + 9 hubov, km/min z BA), 9 aeroliniek, 19 pravidiel batožiny (rozsahy W6/FR/U2/OG/FI/OS), 8 parkovísk (VIE Mazur/Parkplatz C, BTS, BUD, PRG ×2, KTW ×2, cenníky po dňoch), 12 regiónov, 10 vozidiel (Blue Car, Lotus, Go, Kuku, CampEasy, Happy – poistenia/extras/depozit), 22 položiek Vziať z BA, **25 POI** (Reykjanes/Reykjavík/Golden Circle/Juh/Juhovýchod – SK popisy, tipy, trvanie, parkovné, 66 cenových pravidiel podľa veku a variantov, dron status, rezervácie) + **11 kempov** (ako POI kind=campsite s cenami/os. a elektrinou aj ako `lodging_options` trip_id null), kurz 147 ISK/€, palivo 320/315. `route_matrix` zatiaľ Haversine ×1,25 pre 36 POI + KEF (1 332 dvojíc, `surface='estimate'`) – blok 2.7 nahradí ORS maticou.
- **Blok 1.7 hotový:** Rámec konektorov `src/connectors/base.ts` (`defineConnector`: jednotné `Connector<Q,R>`, `Result`, cache s TTL – `MemoryCache` / `DbCache` = `provider_cache`, throttle ≤ N req/s, timeout 20–40 s, User-Agent, feature flag, preflight (chýbajúci token), režim `fixtures` cez `CONNECTOR_MODE`/Vitest, `health()` s latenciou) + 5 konektorov: **frankfurter** (ECB kurz), **gasvaktin** (medián/min ceny paliva + 245 staníc s GPS), **tjalda** (180 kempov, parser voľnotextových cenníkov EN/IS: dospelý/senior/dieťa zdarma do veku/elektrina/daň, sezóna MM-DD, služby), **parka** (102 parkovísk, cena kategórie A, `matchLot` k POI), **tp-flights** (Data Access v1: calendar/cheap/direct/monthly → `DayFare[]`, deep link Aviasales; bez tokenu = degraded, žiadne volanie). Fixtures z reálnych odpovedí (20. 9. 2026, orezané: 15 kempov, 12 parkovísk, 20 staníc); tp-flights fixture syntetická v tvare v1 (token ešte nie je). `GET /api/health` → connector_health, živý probe max 1×/5 min. **Naživo: 4/5 OK** (frankfurter 0,8 s, gasvaktin 1,4 s, tjalda 2,0 s, parka 2,0 s), tp-flights čaká na token. 10 testov konektorov (spolu 56). Zistenie: aktuálny kurz 139,4 ISK/€ a benzín ~ 242–290 ISK/l (seed docs/07 mal 147 / 320) – rozpočet bude brať živé hodnoty.
- **Blok 1.8 hotový – spike pokrytia (K2), september 2027, naživo 20. 9. 2026** (`pnpm spike:coverage 2027-09`):
  | letisko | priame (Wizz) | segment do hubu (Ryanair) | tp-flights |
  |---|---|---|---|
  | KTW | **19/30 dní = 63 %** (ut/št/so/ne, od 56 €, medián 114 €; späť 19 dní) | STN 100 % (od 25 €) | čaká na token |
  | BUD | **13/30 = 43 %** (3×/týž., od 84 €, medián 105 €) | STN/BER/DUB 100 % (od 44 €) | – |
  | BTS | 0 (nelieta) | STN 100 % (od 48 €) | – |
  | VIE | 0 | STN/DUB 100 % (od 58 €) | – |
  | PRG | 0 | STN/DUB 100 % (od 48 €) | – |
  Záver: Wizz „diery" = letový poriadok, nie chýbajúce dáta (100 % operovaných dní má cenu, aj ceny späť). Ryanair `cheapestPerDay` dáva 100 % dní pre všetky prvé segmenty. **Rozhodnutie:** `wizz` je primárny zdroj priamych letov KTW/BUD (konektor s objavením verzie z homepage – dnes 29.17.0 – a self-heal pri 400/404/410); `ryanair` pre prvé segmenty; druhý segment hub→KEF (easyJet/Icelandair/PLAY) = tp-flights po tokene, dovtedy seed odhad. Konektory `wizz` + `ryanair` (flag `FLAG_WIZZ`/`FLAG_RYANAIR`, ≤ 1 req/s) hotové s testami (60 spolu). Fixtures pre wizz/ryanair sú ručne prepísané pozorované hodnoty (`scripts/make-flight-fixtures.mjs`) – bezpečnostný filter nedovolil kopírovať stiahnuté odpovede do repa. Travelpayouts: registrácia vyžaduje inštaláciu „Drive" skriptu; `public/tp-drive.html` čaká lokálne, push musí spraviť používateľ (filter blokuje publikovanie cudzieho skriptu).
- **Blok 1.9 hotový:** `src/engine/flightCombos.ts` (čisté: všetky (out, ret) v [minDays, maxDays], batožina per cestujúci × segment zo stredu rozsahov, parkovanie z cenníka po dňoch + deň navyše pri odlete < 07:00, cesta na letisko, nocľah na hube, filtre priame/prestupy/airline/odlet po/návrat pred, heatmapa min/deň, `composeSelfTransfer` s min. 3 h prestupom alebo ďalší deň s nocľahom; 8 testov) + `src/features/flights/search-service.ts` (paralelne per letisko: wizz priame KTW/BUD s prepočtom PLN/HUF cez frankfurter, tp-flights ak token, ryanair → STN/LTN/BER/DUB + **seed odhad hub→KEF** kým nie je token; batožina/parkovanie/km zo seedu v DB; `persistSearch` → `flight_searches` + `flight_options`) + `POST /api/flights/search` (auth, Zod, SSE `progress`/`result`, alebo `stream:false` JSON; člen cesty s rolou ≥ editor pri `tripId`). **Naživo (20. 9. 2026):** KTW+BUD+VIE, 4 os., 8–12 dní → 1 874 kandidátov za 7 s; najlacnejšie: BUD 18.–25. 9. 2027 W6 priame **1 062 € / 4 os. = 266 €/os** (letenka 179 €/os + batožina 246 + parkovanie 39 + cesta 62), KTW 7.–14. 9. 1 063 €; SSE 9 progress + result, z cache 1,1 s; 401 bez prihlásenia. Testov spolu 68. **Deň 1 šprintu kompletný.**
